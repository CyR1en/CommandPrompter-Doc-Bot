{
  "name": "ref0-pi-capsule",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write src test wire.schema.json package.json tsconfig.json",
    "format:check": "prettier --check src test wire.schema.json package.json tsconfig.json",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.84.4",
    "@earendil-works/pi-ai": "0.84.4",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": "22.19.0"
  }
}
